
#include<iostream>

using namespace std;
int main()

{
	int** arr = nullptr;

int row = 3;
int col = 4;

//first step tol allocate row pointer
arr=(int **)malloc(sizeof(Int *)*row)

//for each row allocate memory for cols

for(int i=0;i<row;i++)

{
	arr[i] = (int*)malloc(sizeof(int) * col);

	for(i-0;i<row;i++)

	{
		for int j = 0;i < col;j++)
		cout << arra[i]; [j] ;

		return 0;
	}
}